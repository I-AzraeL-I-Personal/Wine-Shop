<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('images/winezja.svg') }}" width="200" height="50" alt="Winezja.pl">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">STRONA GŁÓWNA</a>
                </li>
                <li class="nav-item">
                    <a href="/product" class="nav-link">OFERTA</a>
                </li>
                <li class="nav-item">
                    <a href="/about" class="nav-link">KONTAKT</a>
                </li>
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="/login" class="nav-link">LOGOWANIE</a>
                </li>
                {% endif %}
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="/register" class="nav-link">REJESTRACJA</a>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="/cart" class="nav-link">KOSZYK</a>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">KONTO</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="/admin">Panel administracyjny</a>
                        {% endif %}
                        <a class="dropdown-item" href="/account">Informacje</a>
                        <a class="dropdown-item" href="/order">Zamówienia</a>
                    </div>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="/logout" class="nav-link">WYLOGUJ</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>